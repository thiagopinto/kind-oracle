# Frontend Service

Esta pasta conterá o código-fonte da aplicação Frontend (ex: interface React, Next.js ou HTML/JS estático) que responde na rota principal (`/`) da plataforma de testes de webhooks.
