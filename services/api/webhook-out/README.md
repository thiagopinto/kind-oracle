# Webhook Sender Service (webhook-out)

Esta pasta conterá o código-fonte do microsserviço responsável por listar os webhooks recebidos ou realizar disparos externos.
*   **Path associado:** `/api/out`
*   **Método HTTP principal:** `GET`
