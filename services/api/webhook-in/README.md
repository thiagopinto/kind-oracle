# Webhook Receiver Service (webhook-in)

Esta pasta conterá o código-fonte do microsserviço responsável por receber e processar as requisições de webhooks recebidas.
*   **Path associado:** `/api/in`
*   **Método HTTP principal:** `POST`
